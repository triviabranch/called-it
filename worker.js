const ESPN = "https://site.api.espn.com/apis/site/v2/sports/soccer/eng.1/scoreboard";
export default {async fetch(request,env){const url=new URL(request.url);if(url.pathname==="/api/room"&&request.method==="POST"){const id=env.MATCH_ROOM.newUniqueId();return env.MATCH_ROOM.get(id).fetch(new Request("https://room/create",{method:"POST"}));}if(url.pathname.startsWith("/api/room/")){try{return env.MATCH_ROOM.get(env.MATCH_ROOM.idFromString(url.pathname.split("/").pop())).fetch(request)}catch{return new Response("Invalid room",{status:400})}}if(url.pathname==="/play")return env.ASSETS.fetch(new Request(new URL("/game.html",request.url),request));return env.ASSETS.fetch(request)}};
export class MatchRoom {constructor(state,env){this.state=state;this.env=env;this.sockets=new Set();this.room=null}async fetch(request){if(request.headers.get("Upgrade")==="websocket"){const pair=new WebSocketPair();this.state.acceptWebSocket(pair[1]);this.sockets.add(pair[1]);if(!this.room)await this.load();pair[1].send(JSON.stringify({type:"state",state:this.public()}));return new Response(null,{status:101,webSocket:pair[0]})}if(request.method==="POST"&&!this.room){await this.load();await this.save();return Response.json({roomId:this.state.id.toString(),state:this.public()})}return new Response("Room unavailable",{status:404})}async load(){this.room=await this.state.storage.get("room")||{createdAt:Date.now(),lastActivity:Date.now(),home:"Arsenal",away:"Chelsea",score:[0,0],status:"PRE",players:[],predictions:{},events:[{label:"Room opened",detail:"Powered by ESPN"}],provider:{name:"ESPN",league:"eng.1",eventId:null,error:null}}}async save(){this.room.lastActivity=Date.now();await this.state.storage.put("room",this.room)}public(){return {...this.room,predictions:undefined}}broadcast(){const m=JSON.stringify({type:"state",state:this.public()});for(const ws of this.sockets){try{ws.send(m)}catch{}}}async updateESPN(){try{const data=await(await fetch(ESPN)).json();const e=data.events?.find(x=>x.status?.type?.state==="in")||data.events?.[0];if(!e)throw Error("No current ESPN event");const teams=e.competitions?.[0]?.competitors||[],h=teams.find(x=>x.homeAway==="home"),a=teams.find(x=>x.homeAway==="away");this.room.home=h?.team?.displayName||this.room.home;this.room.away=a?.team?.displayName||this.room.away;this.room.score=[Number(h?.score||0),Number(a?.score||0)];this.room.status=e.status?.type?.shortDetail||e.status?.type?.name||this.room.status;this.room.provider.eventId=e.id;this.room.provider.error=null;this.room.events.unshift({label:"ESPN update",detail:this.room.status})}catch(err){this.room.provider.error=err.message}}async webSocketMessage(ws,raw){let m;try{m=JSON.parse(raw)}catch{return}if(!this.room)await this.load();this.room.lastActivity=Date.now();if(m.type==="join"){const p={id:crypto.randomUUID(),name:(m.name||"Supporter").slice(0,20),points:0};this.room.players.push(p);this.room.events.unshift({label:p.name+" joined",detail:"Ready to call it"})}if(m.type==="refresh")await this.updateESPN();await this.save();this.broadcast()}async webSocketClose(ws){this.sockets.delete(ws);if(this.sockets.size===0){await this.state.storage.delete("room");this.room=null}}}
